#class {miphp} {kill}
#class {miphp} {open}

#alias {_player_miphp} {#NOP};

#if {"$guild" == "bard"} {
	#action {^Coffin:%d/%d} {
		#if {!$idle_flag} {
			#var corpses[coffin] %1;
			#line gag
		};
	};
};



#class {miphp} {close}