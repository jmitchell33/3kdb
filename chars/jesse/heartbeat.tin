#class {player_heartbeat} {kill}
#class {player_heartbeat} {open}

#var corpse_delay 1;
#var corpse_use 200;

#alias {_player_heartbeat} {
    #if {!$idle_flag} {
        #if {$bot[active] && $gp1 < 25} {
            .pause;
        };
    };

    #if {$bot[active] && $gp2 < 60} {
        .pause;
    };

    #if {$bot[paused] && "$gp1" == "$gp1_max" && "$gp2" == "$gp2_max"} {
        .unpause;
        ..;
    };
};

#class {player_heartbeat} {close}