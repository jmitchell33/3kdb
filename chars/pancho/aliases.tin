#class {player_aliases} {kill}
#class {player_aliases} {open}

#alias {_player_deadmanActions} {
    #NOP;
};

#alias {_login} {watch mywho;playcult;cult join};

#alias {method-gxp} {
    stance neutral;
    method melee;
    callesquire;
    esq fetch mount;
    chorse;
    mount horse;
};

#alias {method-charge} {
    stance offense;
    callesquire;
    esq fetch mount;
    chorse;
    mount horse;
    method charge;
};

#class {player_aliases} {close}